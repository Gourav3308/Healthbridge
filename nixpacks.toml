[phases.build]
cmds = ["cd backend && mvn clean package -DskipTests"]

[phases.start]
cmd = "cd backend && java -jar target/healthbridge-backend-1.0.0.jar"

[variables]
SPRING_PROFILES_ACTIVE = "production"
